import React from 'react'
import ReactDOM from 'react-dom'
import App from './util/hot-reload'

const root = document.createElement('div')
document.body.appendChild(root)
ReactDOM.render(<App/>, root)
